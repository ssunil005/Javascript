for (let line = "#"; line.length <= 10; line += "#"){
  console.log(line);
  //console.log(line.length());
}
    
// for(let line = "#"; line.length >= 10; line -= "#"){
//   console.log(line);
//   }
// }

